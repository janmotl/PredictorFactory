package connection;

import meta.OutputTable;
import org.apache.log4j.Logger;
import parser.ANTLR;
import run.Setting;

import java.util.HashMap;
import java.util.Map;

public final class SQLHive extends SQL {
	// Logging
	private static final Logger logger = Logger.getLogger(SQLHive.class.getName());



}

