<?xml version="1.0" encoding="utf-8" standalone="yes"?>

<pattern>
	<name>nominal</name>
	<description>Perform dummy coding of a nominal attribute and then return sum. Lovely MySQL do not support 
	pivot hence I have to code it manually. Arity of the column could be checked. Or Chi2 of each "value" could be first
	calculated before construction of the columns.</description>
	<author>Jan Motl</author>
	<date>2014-12-24</date>
	<code>SELECT @baseId
	           , @baseDate
	           , sum(@nominalColumn='@value') "@columnName"
	      FROM @propagatedTable
	      GROUP BY @baseId, @baseDate
	</code>
	<cardinality>n</cardinality>
</pattern>