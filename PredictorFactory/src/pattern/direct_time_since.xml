<?xml version="1.0" encoding="utf-8" standalone="yes"?>

<pattern>
	<name>Direct time since</name>
	<description>Time since the date in the past till the time when the prediction is required (defined with @baseDate).
	Because we are guaranted that there is just one date per the group (cardinality is set to 1), it is wastefull to try
	several aggregates as in "Time since" pattern.</description>
	<author>Jan Motl</author>
	<date>2015-01-10</date>
	<code>SELECT @baseId
	           , @baseDate
	           , @baseTarget
	           , DATEDIFF(@baseDate, @dateColumn) "@columnName"
	      FROM @propagatedTable
	      GROUP BY @baseId, @baseDate
	</code>
	<cardinality>1</cardinality>
</pattern>