<?xml version="1.0" encoding="utf-8" standalone="yes"?>

<pattern>
	<name>Number of children</name>
	<description>
		Number of observations of the child entity with child filter F			
	</description>
	<author>Filip Trojan</author>
	<date>2014-12-25</date>
	<code>SELECT @baseId, @baseDate, @baseTarget, coalesce(count(*),0) "@columnName"
	      FROM @propagatedTable
	      GROUP BY @baseId, @baseDate
	</code>
	<cardinality>n</cardinality>
</pattern>
